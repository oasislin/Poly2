# AGENTS.md

本仓库：Polymarket 温度预测系统（Phase 1，高精度物理概率模型）。当前执行规格以《项目执行文件 v5.9.2(细化版).md》为准（已对齐 v5.9.2，无冲突）；需求来源《项目方案：Polymarket 温度市场量化投注系统 (v2.4 Final).md》。

## 规格更新流程（触发即执行）

当用户出现以下任一情形时，**执行 `docs/spec-update-process.md` 的完整流程**：

- 提供新版本《项目执行文件》（如 v5.8）或《项目方案》，并要求"走流程"/"走标准流程"/"规格更新"
- 说"grill with docs"、"to spec"、或"更新规格文档"

流程四步：文档审读 → grill-with-docs（grilling + domain-modeling，产出 ADR/术语表）→ to-spec/to-tickets（生成 spec + 拆分 ticket + 发布命令）→ 冻结规则校验。发布命令生成后**交给用户执行**，agent 不代跑 gh。

## 硬性规则

- **严禁未经明确授权擅自修改代码（最高铁律）**：任何时候，在用户没有给出**明确的代码修改指令**（如明确说“开始修改”、“去写代码”、“按方案执行”）之前，**绝对不得擅自创建、修改或调整任何项目代码及脚本**。遇到需求、提议或错误排查时，**只能先讨论方案、确认思路**，必须在用户明确确认并授权后，才能进入代码编写阶段。
- **原始观测结果不可篡改（最高铁律）**：凡由代码、脚本、测试、API 或外部工具产生的数值、状态与日志（评分、指标、测试结果、退出码等）均为客观观测值，agent 只允许**原样读取、原样汇报**，严禁修改、重估、四舍五入、美化、补全或"解释性修正"。观测值与来源输出任何不一致即属违规：立即终止当前任务，向用户报告违规，不继续后续流程。
- **"通过/达标/完成"声明必须附执行证据（硬性）**：声称测试通过、评分达标、验收满足时，必须同条输出：①实际执行的命令或脚本路径；②输出末尾关键片段（含原始数值）；③判定阈值与判定逻辑。**未执行的测试不得表述为"通过"**，只能写"未运行测试，结论待验证"。无证据的通过声明视为违规，评审默认无效。
- **判定必须两步分离（硬性）**：禁止跳过原始值直接给结论，禁止先写结论再回改数值。固定输出格式：`原始观测值 = <原样值>；判定阈值 = <目标值>；结论 = 通过/不通过`。结论只能由"原始值 vs 阈值"机械比较得出，不得掺入模型主观判断。
- **判定下沉到代码（硬性）**：凡能写成 `pass = value >= threshold` 的判定，一律在代码/脚本中实现为布尔结果，agent 只读取并引用该结果，**不自行做数值比较，也无权重算该布尔值**。
- **输出前自检（硬性）**：任何含"通过/达标/评分"的输出，交付前必须自检：①汇报值与工具原始输出逐字一致；②测试确已执行且证据齐全；③结论由原始值比较得出。任一不满足，禁止交付，改为终止任务并上报用户。
- **Git Push 严格遵从人工明确指令（硬性）**：严禁在每次代码修改后自动执行 `git push` 同步到 GitHub。代码推送操作**必须且仅在用户给出明确指令（如“push 到 GitHub”、“同步远端”）时方可执行**。
- **专业与技术描述严禁自由发挥（硬性）**：所有专业描述、分类标签（如气候分型、台站特征、统计指标等）、技术术语和口径命名，**必须有据可依**（如项目受控词表、代码现有元数据常量、权威科学标准），**严禁凭借大模型常识或语感自由发挥、随意拼凑非受控形容词**（Uncontrolled Vocabulary）。若非项目正式定义的法定字段，严禁在正式质检报告、架构文档或数据表中擅自捏造列名或属性。
- **已完成的任务是冻结的历史记录**（如 task 文件中的 Task 1.1），内容永不改写；只更新未完成任务的描述与相关设计文档。
- 设计文档更新采用**版本标注**（"已对齐 vX.Y"），不重写既有内容。
- 项目内部文档版本号由 agent 决定（核心架构变更 → 次版本 +1；细节修正 → 修订号 +1），并向用户报告；用户提供的输入文档版本号由用户定。
- 用 `gh` 代跑（如 issue 验收/关闭）遇认证或权限报错时，**不得反复重试**：先请用户完成认证后再重跑一次；持续报错则停止。
- **Task 与 Ticket 显式全拼命名原则（Phase 2 起强制）**：为避免跨阶段编号混淆，Phase 2 起所有新任务的 GitHub Issue 与 Ticket 必须采用**显式全拼前缀拼接格式**：
  - Spec 主 Issue：`Spec: Phase X Task YY - <描述>`（如 `Spec: Phase 2 Task 01 - Active 10 站生产级概率分布模型重训与三重验收`）
  - 切片 Ticket Issue：`Phase X Task YY - Ticket ZZ: <type>(<scope>): <描述>`（如 `Phase 2 Task 01 - Ticket 01: feat(modeling): ...`）
  - 严禁使用容易引起跨阶段歧义的孤立简称（如单独的 `Ticket 01` 或隐式缩写）。
  - **历史向前兼容**：既往已完成、已冻结的历史 Issue 与文档（Phase 1 的 2.x/3.x/4.x，调查单的 Ticket 01~05，Phase 1.5 的 Task 01~09）保持原貌，不作破坏性回改，杜绝引用断裂。
- **严禁脱离客观工程现实与数据资产凭空捏造前置条件（硬性）**：任何方案、RFC、验收门禁或评审回复，**必须严格基于当前代码库、现有数据资产及客观工程现实**。严禁凭空预设不存在的数据资产（如未采购采集的 2020+ GEFS 预报）、未接入的外部生产基础设施（如当前未配置密钥与长连接的 WSS 生产盘口）、或脱离统计与几何常识的虚幻指标（如在 2°F 单档位上承诺 40%~60% 离散概率、要求多个独立分桶 100% 落入 95% 置信区间等反数学谬误）。所有论据、前置依赖和门禁指标必须实事求是、数理自洽、物理可行。**任何 RFC/ADR/答辩中的实证性数字声明，必须附带可复现工件引用（脚本路径 + 数据文件校验和 + 复现命令）；无工件引用的实证声明，评审默认无效，且视为违反本条硬性规则。本规则同样适用于执行结果：不得以假设、印象或模型记忆代替实测值。**

## 代码生成约束

- 修改文件时避免重写整个文件，始终优先使用 `edit` 工具进行细粒度、精确的 diff 修改。
- 若文件创建或重构的规模很大，将任务拆分为多个步骤/函数分步完成。
- 工具调用前后的注释与说明保持简洁。

## 数据管道验证工作流（Task 1.2 GEFS，硬性）

- **每张 ticket 完成时必跑真实冒烟**：`RUN_NETWORK_TESTS=1 python -m pytest tests/unit/data_acquisition/test_gefs_fetcher.py::test_network_reforecast_single_message -q`，**且交付说明必须附命令与输出末尾片段**，未附视为未完成。ticket 不涉及网络路径时可豁免，但须在交付说明中注明理由。**mock 全绿 ≠ 真实网络可用**（T01 与 T02 均已实证：mock 绿但真实 AWS 路径坏）。
- **外部契约必须钉成契约测试**：GRIB idx 搜索正则、真实解码变量名（tmax/tmin）、网格方向/分辨率等，必须用「观测到的真实数据字面量」写成快速单元测试（参考 `TestBuildSearch`），mock 不得覆盖这些契约。
- **mock 仅 mirror 已验证的契约**：`MockHerbie` / `make_fake_forecast_ds` 只反映真实联网验证过的行为，不得凭空假设。

## 其他

- 开发协作、沟通模式（提问/执行/确认）见 `DEVELOPER_GUIDELINES.md`。
