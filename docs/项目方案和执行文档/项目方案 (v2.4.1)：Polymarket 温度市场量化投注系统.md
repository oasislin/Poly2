---
created: 2026-09-02 11:29:52
updated: 2026-09-02 11:30:25
---

# 项目方案 (v2.4.1)：Polymarket 温度市场量化投注系统

（对齐执行文件 v5.9.2：高斯 EMOS、5 成员集合、气候学基准、三重验收；§6 三项待确认项全部代码核验闭环；§5 陈旧阈值同步为 v1.3 两档口径）

## 0. 变更日志

|版本|日期|变更内容|
|---|---|---|
|v2.2|2026-08-11|初版：Wunderground 为真值，偏态 EMOS，四项验收指标|
|v2.3|2026-08-20|四处推翻级对齐（高斯 EMOS / 5 成员 / 气候学基准 / 三重验收）；新增 §6 三项 Pending 待确认区|
|v2.4|2026-08-20|§6 三项 Pending 全部经代码核验闭环并回填正文；新增告警-降级事件契约、单位归一化强制关卡等 11 项观察点落位；Pending 区清空归档|
|**v2.4.1**|2026-08-20|**§5 陈旧监控条款修订：单一“固化 3.0h”更新为与 Phase 2 执行文件 v1.3 一致的两档阈值（常规 3.0h / 切换窗 0.75h）；新增“参数权威权属声明”——阈值数值、计时语义与幂等规则以执行文件 v1.3 §5.2 为权威，本文档保持概念级描述，不重复维护参数**|

> **⚠️ 参数复用警告**（延续 v2.3）：v2.2 时代已训练的偏态模型或 31 成员方差参数全部作废，严禁迁移至 v5.9.2 管道。

## 1. 项目目的与战略定位

**核心目标**：构建针对 Polymarket 温度市场的**连续概率预测系统**，输出最高温与最低温的概率分布，用于投注决策。

**战略分阶段原则**：

- **Phase 1（当前）**：高精度物理概率模型，Wunderground 历史数据为真值，不考虑盘口与资金管理。
- **Phase 2（后续）**：Phase 1 验收后引入盘口流动性、滑点控制与资金管理（详见《Phase 2 执行文件 v1.3 Engineering-Spec Final》）。

## 2. 模块一：数据架构

### 2.1 观测数据（真值与历史统计）

- **数据源**：Wunderground 历史页面（真值标签、气候学 Floor 唯一口径、回测结算标签）。
- **获取工具**：`requests` + `BeautifulSoup`，解析 “Day High” / “Day Low”。

### 2.2 预报数据（特征源）

- **数据源**：GEFS，**仅 5 成员 c00+p01-p04，11/31 成员一律忽略**；特征收敛为集合均值 TˉensTˉens​ 与集合标准差 SensSens​。
- **日极值窗口**：6h 窗口，完全包含 ⊆ 本地日，弃 3h。
- **获取工具**：Herbie + `xarray`/`cfgrib`；`xr.merge`/`xr.concat` 必须声明 `compat='override', coords='minimal'`。

### 2.3 实时数据（实战用）

- **Phase 1**：采用**参数依赖注入**——`current_temp` / `observation_time` 经 API/CLI 显式传入（`DynamicCorrector` 纯内存闭式运算，回测可确定性重放）。
- **Phase 2**：接入 **METAR 分钟级实时流（高频截断，常规时段生效源）+ Wunderground（次日权威结算 + 切换窗截断源）双轨解耦**。
- **站点对齐**：中央字典 `STATION_METADATA`（`constants.py`）统一 ZSPD/KDEN 的经纬度、高程、时区、单位与 Polymarket ID。
- **脏数据清洗**：`DataValidator` 强制物理边界 [−60°C,+60°C][−60°C,+60°C] 拦截。**[v2.4 新增强制关卡]** 所有观测值进入边界检查前，必须先经 `STATION_METADATA` 单位字段归一化为摄氏（防止丹佛华氏输入被误拦截或漏拦截）；配单测 `test_fahrenheit_input_normalized_before_physical_check`（Phase 2 Milestone 2.1 阻塞项）。

## 3. 模块二：动态概率预测模型（三层架构，均已代码核验）

### 3.1 第一层：静态基础模型（高斯 EMOS）

- 均值 μ=a+b⋅Tˉensμ=a+b⋅Tˉens​；方差（平方参数化）σ2=c2+d2Sens2+σclim2(d)σ2=c2+d2Sens2​+σclim2​(d)。
- 气候学 Floor：仅站点实测、2000-2018 严格 OOS、31 天窗 × 19 年逐日平滑，不参与优化。
- 40 组参数（20 模型/站 × 2 站）；TMAX 节点 {6h,30h,54h}{6h,30h,54h}，TMIN 节点 {24h,48h}{24h,48h}；仅 00Z 起报，`round_to_nearest_6h` 归桶，名义目标 15:00/06:00 LT。
- 训练：CRPS 闭式解 + L-BFGS-B；L2 仅对 dd（λ=10−3λ=10−3）；热启动 (0,1,0,1)(0,1,0,1)+扰动。
- 降级：硬触发（未收敛 / NaN/Inf）→ Level 2 气候学；软触发（CRPS 劣于气候学 p<0.05p<0.05）；∥c∥,∥d∥>10∥c∥,∥d∥>10 仅告警。

### 3.2 第二层：动态修正层（动态截断层）

- 截断修正：TMAX P(X≥L∣X>Tnow)P(X≥L∣X>Tnow​)；TMIN P(X≤L∣X<Tnow)P(X≤L∣X<Tnow​)。
- 插值/外推：TMAX 内插节点 {12,18,24,36,42,48}{12,18,24,36,42,48}；TMIN<24h 借用 24h 参数 + σL/24σL/24​ 衰减；极短时效截断层接管主导权。
- **缺测容灾**：TnowTnow​ 为 None/NaN 时透明回退静态先验（`is_truncated=False`）。该回退必须配对上报 `WARNING` 级事件（区别于 ERROR 级陈旧告警），禁止静默降级。
- **触发范式（混合制，代码核验闭环）**：
    - **Phase 1**：CLI/API 按需同步触发，无常驻调度器（截断计算 <1ms，纯内存闭式运算）。
    - **Phase 2**：METAR 观测事件 + 盘口推送事件双驱动；节流采用**或逻辑双重防抖**——∣Tnew−Tprev∣≥0.1°C∣Tnew​−Tprev​∣≥0.1°C **或** Δt≥60sΔt≥60s。**周期刷新（60s）具有真实计算意义**：`ConstraintEnforcer` 的可达区间随 ΔtΔt 收缩，即使气温不变也须重算，严禁以“气温没变”为由裁剪周期触发。
    - **告警冷却与计算节流隔离**：告警层冷却（MD5 语义指纹，3600s）与计算触发节流为**两套独立机制，严禁复用**——告警节流防刷屏，计算节流省算力，混用将导致极短时效窗口截断严重滞后。
    - 配置两态：`min_reprice_edge` 基线 0.03 / 数据陈旧降级态 0.05，联动固化，不得静态单值。
    - `configs/default.yaml` 预留 `trigger:` 区块骨架（Phase 2 schema 锚点）。

### 3.3 第三层：物理硬约束（独立保留，代码核验闭环）

- **独立顶级组件** `ConstraintEnforcer`（`src/prediction/constraint_enforcer.py`），架构与 StaticPredictor / DynamicCorrector / BinConverter 平级，位于预测栈第 3 层（先验 → 截断 → **物理硬拦截** → 档位转换），对前序输出具绝对覆盖优先级。
- **机理**：计算距名义极值时刻（TMAX 15:00 / TMIN 06:00 LT）剩余时间 Δt=max⁡(0,tpeak−tnow)Δt=max(0,tpeak​−tnow​)；基于站点季节历史升/降温速率计算物理可达区间 [Tnow−rcoolΔt, Tnow+rwarmΔt][Tnow​−rcool​Δt, Tnow​+rwarm​Δt]；超限档位硬拦截（概率强制 0.0）；CDF 全域单调不减。
- **速率口径**：基于站点季节历史速率的 **99.9% 分位数**（工程化准最大值，v2.2 的“绝对最大”表述据此修正）；配置层保留强制覆盖开关，允许人工注入绝对极值。
- **边界语义**：Δt=0Δt=0（越过名义极值时刻）时可达区间坍缩为闭区间单点，**属设计决策而非缺陷**；固化单测 `test_deltat_zero_after_peak_collapse` 防止误“修复”。
- **缺测容灾**：实况缺测时标记 `is_constrained=False` 优雅回退，不阻断预测。
- **Phase 2 兼容性**（详见执行文件 v1.3 §6 规范 4）：概率硬置 0 的档位流入凯利 SLSQP 时，pipi​ 必须保持在乘法侧为零的写法（严禁预计算 ln⁡pilnpi​）；pmodel≥0.995pmodel​≥0.995 的确定性档位施加额外下注上限。

## 4. 模块三：验证引擎（三重验收）

### 4.1 交付物标准

预测结果文件（时间、站点、PDF 参数、Wunderground 真值）+ 分 TMAX/TMIN 的统计校准报告。

### 4.2 三重验收指标

**① 标准节点**：CRPS；PIT K-S 检验 p>0.05p>0.05；**CRPSmodel<CRPSclimCRPSmodel​<CRPSclim​（门禁级）**。  
**② 插值节点（留出节点法）**：30h 留出，6h & 54h 重建；双断言：CRPSvirtual≤1.05×CRPSrealCRPSvirtual​≤1.05×CRPSreal​ 且 PIT K-S p>0.05p>0.05。  
**③ 极端压力测试**：阈值取 2000-2018 的 90th/10th 分位；样本仅取 2019 超阈值部分；双底线：CRPSmodel<CRPSclimCRPSmodel​<CRPSclim​ 且 90% CI 覆盖率 ≥80%≥80%。

### 4.3 废弃声明

- **MPIW**：废弃。**Talagrand 图**：废弃，职能由“PIT K-S + 覆盖率断言”替代，验收脚本不得实现。

**判定规则**：三重验收全部通过，方允许启动 Phase 2 资金投入。

## 5. 数据源故障联动与告警契约

- **陈旧度概念定义**：任一实时源的陈旧度恒定为 tnow−tlast_observationtnow​−tlast_observation​（最新观测数据时间戳的龄期），与监测对象切换、窗口边界无关，计时器跨时段连续。
- **陈旧监控与阈值档位（v2.4.1 修订）**：`check_data_staleness()` 采用**两档阈值**——常规时段（生效截断源 METAR）**3.0h**；结算前口径切换窗口（名义极值时刻前 2h，生效截断源 Wunderground）**0.75h**（高于 Wunderground 实况页 15~30 分钟自然更新上界 + 余量，防窗口入口假阳性）。代码默认值 6.0h 须改配置覆盖。
- **[v2.4.1 新增] 参数权威权属声明**：阈值数值、计时语义细则、幂等规则与恢复稳定期的**权威定义位于《Phase 2 执行文件 v1.3 Engineering-Spec Final》§5.2**，本文档保持概念级描述，不重复维护参数。后续任何阈值调整仅修订执行文件，本文档不再同步维护具体数值。
- **告警-降级事件契约**：`DATA_STALENESS`（ERROR 级）是 Phase 2 降级状态机的**唯一触发信号源**，触发后：动态截断层停用、Effective σ × 1.3、EV 门槛 0.03 → 0.05。降级动作幂等，重复事件不叠加系数（细则见 v1.3 §5.2）。
- **降级加权参考**：截断层缺测回退的 `WARNING` 级事件（§3.2）作为 Phase 2 降级加权的辅助信号，不直接触发降级。
- **风险边界澄清**：结算源（Wunderground 日极值）可用性故障属另一风险类别，由 Phase 2 僵尸仓位隔离机制管辖，不归陈旧降级管辖。
- **结算前口径切换**：进入名义极值时刻前 2h 窗口，截断层输入源由 METAR 切换为 Wunderground 实况，消除仪器口径差在档位边界附近的翻转风险（Phase 2 落地）。

## 6. 已归档核验记录（原 Pending 区）

> 三项 Pending 已全部经代码核验闭环并回填正文（§2.3 / §3.2 / §3.3 / §5），本区转为核验档案：

|#|结论|代码位置|观察点去向|
|---|---|---|---|
|1|物理硬约束层：独立保留|`src/prediction/constraint_enforcer.py`|速率口径→§3.3；Δt=0 单测→Phase 1 单测清单；凯利兼容→v1.3 §6 规范 4|
|2|实况源：参数依赖注入，Phase 2 METAR/Wunderground 双轨|`dynamic_corrector.py` / `constants.py`|单位归一化→**Phase 2 M2.1 阻塞项**；阈值+事件契约→§5；口径切换→Phase 2 落地；回退告警→§3.2|
|3|触发范式：混合制（按需同步 / 双事件驱动）|`run_predictions.py` / `main_pipeline.py`|或逻辑防抖+冷却器隔离+两态 edge→§3.2；配置骨架→default.yaml|

## 7. 实施路线图

**Phase 1 收尾（Step 1.5）**：固化 3.0h 常规阈值配置；实现缺测回退 WARNING 配对；补 `test_deltat_zero_after_peak_collapse` 与 `test_fahrenheit_input_normalized_before_physical_check` 单测；`default.yaml` 预留 `trigger:` 骨架。

**Phase 2 启动**：按《Phase 2 执行文件 v1.3》执行，其 §七测试套件作为 CI/CD 验收门禁。